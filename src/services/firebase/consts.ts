export const COLLECTIONS = {
  USERS: "users",
  TRANSLATIONS: "translations",
  MESSAGES: "messages",
} as const;

export const DEFAULT_USER_PREFERENCES = {
  theme: "light",
  notifications: true,
} as const;
